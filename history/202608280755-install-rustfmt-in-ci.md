# Install rustfmt in CI / 在 CI 安装 rustfmt

## 中文

- Nightly toolchain 显式安装 `rustfmt`，使新增的 `cargo fmt --check` 守门可执行。

## English

- Install `rustfmt` explicitly for the nightly toolchain so the new `cargo fmt --check` gate can run.
