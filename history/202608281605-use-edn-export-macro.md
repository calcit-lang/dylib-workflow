# 使用同步 EDN 导出宏 / Use the synchronous EDN export macro

## 中文

- 升级 `calcit_native_ffi` 到 0.1.1。
- 使用公共宏生成 `path_exists_calcit_ffi_v1`，删除最后一层手写 C wrapper。

## English

- Upgrade `calcit_native_ffi` to 0.1.1.
- Generate `path_exists_calcit_ffi_v1` with the shared macro and remove the final handwritten C wrapper.
