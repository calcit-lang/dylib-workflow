# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将 native dylib 模板固定到 Calcit 0.15.3，不修改模板元数据版本。
- 使用 `surface-latest-v2` 检查完整 Snapshot，并应用编译器能够证明安全的语法优化。
- 保持共享 `calcit_native_ffi` C-safe buffer ABI 示例不变。

## English

- Pin the native dylib template to Calcit 0.15.3 without changing its metadata version.
- Check the full Snapshot with `surface-latest-v2` and apply compiler-proven syntax cleanup.
- Preserve the shared `calcit_native_ffi` C-safe buffer ABI example.

## 验证 / Verification

- strict dependency, Calcit type/quality, and dynamic-method gates
- Rust format, tests, strict Clippy, and release build
- C-safe export audit and native Calcit behavior run
